spring:
  application:
    name: image-search-mcp-server
  profiles:
    active: stdio
server:
  port: 8127
logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
